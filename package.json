{
  "name": "atelier-payroll",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Weekly payroll app for a small tailoring shop (invoice photo -> payout sheet)",
  "scripts": {
    "start": "node server/index.js",
    "build": "npm --prefix client run build",
    "icons": "node tools/gen-icons.js",
    "migrate": "node tools/migrate-to-cloud.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.104.1",
    "@libsql/client": "^0.15.0",
    "@vercel/blob": "^1.1.1",
    "express": "^5.2.1"
  }
}
